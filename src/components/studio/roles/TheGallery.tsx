'use client';

import React from 'react';

const TheGallery = () => {
  return (
    <div>
      <h1>The Gallery (Guest)</h1>
      {/* Future UI for live feed, nudge button, etc. */}
    </div>
  );
};

export default TheGallery;
