'use client';

import React from 'react';

const CommandCenter = () => {
  return (
    <div>
      <h1>Command Center (Host)</h1>
      {/* Future UI for participant health, recording toggles, etc. */}
    </div>
  );
};

export default CommandCenter;
